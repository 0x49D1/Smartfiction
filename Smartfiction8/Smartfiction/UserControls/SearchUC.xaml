<UserControl x:Class="Smartfiction.UserControls.SearchUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}" Height="Auto">

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}" Height="150">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="txtSearch" Grid.Row="0" Grid.ColumnSpan="2" />
        <Button Name="btnSearch" Grid.Row="1" Grid.Column="0" Click="BtnSearch_OnClick">Search</Button>
        <Button Name="btnCancel" Grid.Row="1" Grid.Column="1" Click="BtnCancel_OnClick">Cancel</Button>

    </Grid>
</UserControl>